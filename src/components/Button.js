
export const Btn = ({children}) => {
  return (
    <button className="w-52 bg-gradient-to-r from-gray-500 via-slate-50 to-gray-700 text-black dark:bg-white text-gray-500 p-2 border-1 rounded-lg font-medium hover:bg-gradient-to-t hover:from-red-200 hover:via-slate-50 hover:to-red-400">{children}</button>
  )
}
